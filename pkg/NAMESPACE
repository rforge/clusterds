import(rJava)

export(cluster,
       DenStream,
       CobWeb,
       Clustream,
       convertParams,
       create_stream,
       get_instance)

S3method(print, ClusterDS)
S3method(plot, ClusterDS)
