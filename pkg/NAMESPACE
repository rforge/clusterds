import(rJava)
import(MASS)

export(

	# DSC_MOA
	DSC_DenStream,
	DSC_CobWeb,
	DSC_Clustream,
	DSC_ClusTree,
	DSC_StreamKM,
	
	# DSC_R
	DSC_tNN,
	
	DSD_MOA,
	DSD_Gaussian_Static,
	DSD_Gaussian_Dynamic,
	DSD_ReadStream,
	
	write_stream,
	cluster,
	get_centers,
	get_points,
	nclusters,
	plot
      )

S3method(plot, DSC)
S3method(print, DSC)
S3method(print, DSD)

S3method(write_stream, DSD)

S3method(get_centers, DSC_MOA)
S3method(get_centers, DSC_tNN)

S3method(get_points, DSD_MOA)
S3method(get_points, DSD_Gaussian_Static)
S3method(get_points, DSD_Gaussian_Dynamic)
S3method(get_points, DSD_ReadStream)
