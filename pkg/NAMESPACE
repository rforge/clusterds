import(rJava)

export(cluster,
       DenStream,
       CobWeb,
       Clustream,
       convertParams,
       create_stream,
       create_rStream,
       get_centroids,
       get_instance)

S3method(print, ClusterDS)
S3method(print, DS)
#S3method(get_instance, DS)
#S3method(get_instance, rDS)
