import(rJava)
import(MASS)

export(
	# DSC_MOA
	DSC_DenStream,
	DSC_CluStream,
	DSC_ClusTree,
	
	# DSC_R
	DSC_tNN,
	DSC_Wrapper,
	
	# DSD
	DSD_RandomRBFGeneratorEvents,
	DSD_Gaussian_Static,
	DSD_ReadStream,
	DSD_Wrapper,
	
	write_stream,
	cluster,
	get_centers,
	get_points,
	nclusters,
	reset_stream,
	evaluation,
	recluster,
	testClusters
      )

S3method(plot, DSC)
S3method(print, DSC)
S3method(print, DSD)
S3method(print, DSD_Wrapper)

S3method(write_stream, DSD)

S3method(get_centers, DSC_MOA)
S3method(get_centers, DSC_tNN)

S3method(get_points, DSD_Gaussian_Static)
S3method(get_points, DSD_ReadStream)
S3method(get_points, DSD_Wrapper)
S3method(get_points, DSD_MOA)

S3method(reset_stream, DSD_Wrapper)
S3method(reset_stream, DSD_ReadStream)
